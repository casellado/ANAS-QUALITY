/**
 * PDF Engine — jsPDF templates per verbali ANAS.
 * I template sono identici ai prestampati cartacei ANAS.
 *
 * Ogni modulo fornisce la propria funzione `generaPdf(verbale: Verbale) => Promise<Blob>`
 * nel campo TipoVerbale.generaPdf. Questo package fornisce gli helper condivisi.
 */

export { /* placeholder — helper PDF verranno aggiunti qui */ }

